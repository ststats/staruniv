{
  "name": "staruniv",
  "private": true,
  "description": "테스트 실행용(node --test). 달력 캡처는 scripts/capture_calendar.py가 크롬으로 찍는다.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
