[pytest]
; scripts/ 안에 test_로 시작하는 파일이 실수로 들어가도(예: 로컬에서 임시로 복사해둔
; 파일) pytest가 tests/ 밖은 아예 안 훑도록 제한한다. 이게 없으면, 이름이 같은
; test_*.py 파일이 서로 다른 폴더에 있을 때 pytest가 "import file mismatch" 에러로
; 전체 수집을 중단시켜버린다(각 폴더에 __init__.py가 없어서 모듈 이름이 파일명
; 하나로만 결정되기 때문).
testpaths = tests
