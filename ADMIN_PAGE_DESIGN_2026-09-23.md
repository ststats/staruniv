# StarUniv 어드민 페이지 구성 설계

작성일: 2026-09-23  
대상: StarUniv 관리자 페이지와 `ststat` 운영 파이프라인

## 1. 설계 목표

어드민은 단순 CRUD 목록이 아니라 다음 세 질문에 바로 답할 수 있어야 한다.

1. 데이터가 정상적으로 갱신되고 있는가?
2. 지금 수정한 값은 언제 공개 사이트에 반영되는가?
3. 문제가 생겼을 때 어느 작업과 어느 데이터를 확인해야 하는가?

현재 페이지는 기능 자체는 대부분 갖췄지만 메뉴가 사이트 화면 기준으로 나뉘어 있고, 운영 상태와 편집 기능이 한 스크립트에 모여 있다. 다음 구조는 운영 목적을 기준으로 메뉴를 다시 묶고, 공개 화면 미리보기와 데이터 편집을 함께 유지하는 안이다.

## 2. 권장 메뉴 구조

### 2.1 운영 현황

첫 화면은 총건수보다 데이터 상태를 우선한다.

- 최근 외부 크론 실행 상태
- 마지막 성공·실패 시각
- 방송통계 기준일과 갱신 시각
- ELO 원본 마지막 경기일
- 활성 랭킹 스냅샷 기준일과 계산 버전
- 웹사이트 마지막 빌드 시각
- 실패한 작업과 오류 요약
- 즉시 다시 실행할 수 있는 외부 크론 또는 GitHub Actions 링크

상태 표현:

| 상태 | 기준 | 표시 |
|---|---|---|
| 정상 | 예상 갱신 주기 안에 성공 | 초록 |
| 지연 | 예상 주기 초과, 최근 성공 존재 | 노랑 |
| 실패 | 최근 실행 실패 | 빨강 |
| 실행 중 | 현재 작업이 진행 중 | 파랑 |

### 2.2 선수·팀

- StarUniv 멤버
- 전체 티어 선수
- 팀·소속
- 휴면·FA
- 승급 이력
- 신규·변경 후보 검토
- 중복 ELO ID·SOOP ID 검사

목록 상단에는 검색, 소속, 티어, 종족, 활동 상태 필터를 둔다. 선수 편집 화면에는 데이터 출처와 반영 범위를 함께 표시한다.

### 2.3 경기·랭킹

- StarUniv 경기와 세트
- ELO 경기 조회
- 티어 랭킹
- 티어 괴리 검토
- 원본 연결 오류
- 재계산 영향 범위

티어 괴리 화면은 단순 현재 티어·제안 티어만 보여주지 않고 최근 경기 수, 신뢰구간, 주요 상대 티어 전적, 마지막 승급일을 함께 보여준다. 관리자가 승인한 결과만 티어 명단에 반영한다.

### 2.4 일정·콘텐츠

- 일정
- 휴방
- 연혁
- 홈 설명
- 내비게이션
- 외부 도구

공개 페이지 미리보기에서 항목을 선택하면 오른쪽 편집 패널이 열리는 현재 방식은 유지한다. 저장하지 않은 변경이 있으면 페이지 이동 전에 경고한다.

### 2.5 영상

- 수집 채널
- 수집 영상
- 추천 영상
- 숨김 처리
- 마지막 동기화 상태
- 채널별 오류

영상 채널 설정은 `다음 동기화 후 반영`, 추천·숨김은 `즉시 반영`으로 구분한다.

### 2.6 설정·권한

- 사이트 설정
- 표시할 방송통계 지표
- 관리자 계정과 역할
- 변경 이력
- 외부 크론·워크플로 연결 상태

## 3. 전체 화면 배치

```text
┌──────────────────────────────────────────────────────────────┐
│ 로고  운영 현황  선수·팀  경기·랭킹  콘텐츠  영상  설정     │
├───────────────────────────────┬──────────────────────────────┤
│                               │ 편집 패널                    │
│ 공개 사이트 미리보기          │ 제목·상태                    │
│ 또는 관리 목록                │ 입력 필드                    │
│                               │ 반영 시점 배지               │
│                               │ 변경 영향                    │
│                               │ 취소 / 저장                  │
├───────────────────────────────┴──────────────────────────────┤
│ 작업 결과 · 오류 · 다음 반영 시점                            │
└──────────────────────────────────────────────────────────────┘
```

데스크톱에서는 미리보기와 편집 패널을 나란히 배치하고, 모바일에서는 목록 → 편집 화면 순서로 전환한다.

## 4. 운영 현황 대시보드

### 최상단 상태 카드

- `방송통계`: 기준일, KST 갱신 시각, 지연 시간
- `ELO 원본`: 마지막 경기일, 총 경기 수
- `티어 랭킹`: 활성 스냅샷, 계산 버전, 랭킹 선수 수
- `웹 빌드`: StarUniv·Synergy 마지막 성공 시각
- `외부 크론`: 마지막 호출, 결과, 다음 예정 시각

### 주의가 필요한 항목

- 실패한 파이프라인
- 일정 시간 이상 `running` 상태인 작업
- 오늘 데이터 행 수가 평소보다 급감한 경우
- 티어 명단과 ELO 선수 연결 실패
- 승급 이력 오류
- 티어 괴리 상위 후보
- 영상 수집 실패 채널

### 최근 변경

- 작업자
- 변경 시각(KST)
- 메뉴와 대상
- 변경 전후 요약
- 즉시 반영 여부
- 재계산·웹 빌드 실행 여부

## 5. 저장과 공개 반영 규칙

모든 편집 화면의 저장 버튼 근처에 반영 시점을 표시한다.

| 배지 | 의미 | 예시 |
|---|---|---|
| 즉시 반영 | 브라우저가 Supabase에서 직접 읽음 | 티어 명단, 영상 숨김 |
| 다음 데이터 갱신 | `ststat` 재계산 필요 | 경기 수정, 승급일 수정 |
| 다음 웹 빌드 | 정적 JSON·HTML 재생성 필요 | StarUniv 내부 경기·멤버 셸 |
| 관리자 승인 | 자동 후보를 검토 후 적용 | 티어 괴리, 신규 선수 |

저장 완료 메시지도 `저장했습니다`로 끝내지 않고 다음 동작을 알려준다.

```text
경기를 저장했습니다. 다음 ststat 실행에서 통계와 티어 랭킹이 다시 계산됩니다.
```

## 6. 주요 화면 상세

### 선수 목록

- 서버 검색과 페이지네이션
- 이름·닉네임·SOOP ID·ELO ID 통합 검색
- 티어·종족·소속·활동 상태 필터
- 중복 ID 경고
- 최근 경기일과 데이터 갱신일
- 다중 선택 후 소속·상태 일괄 변경

### 경기 목록

- 100건 단위 서버 페이지네이션
- 날짜 범위, 상대팀, 형식, 결과 필터
- 경기와 세트를 한 트랜잭션으로 저장
- 수정 시 재계산되는 통계 범위 표시
- 원본 경기와 연결되지 않은 라운드 경고

### 티어 랭킹

- 티어별 현재 순위
- 30·90·365일 경기 수와 승률
- 보수 레이팅과 원시 레이팅
- 신뢰구간
- 데이터 제안 티어
- 현재 티어와의 차이
- 후보 승인·보류·기각
- 승인 사유와 작업자 기록

### 파이프라인

- 작업별 최근 실행 목록
- 읽은 행·쓴 행·건너뛴 행
- 소스 커서
- 스냅샷 ID
- 캐시 재사용 여부
- 오류 전문 복사
- 외부 크론 실행 링크

## 7. 권한 설계

권장 역할:

| 역할 | 권한 |
|---|---|
| viewer | 조회만 가능 |
| editor | 일정·콘텐츠·영상 편집 |
| data_manager | 선수·경기·티어 데이터 편집과 재계산 요청 |
| admin | 계정·권한·설정 포함 전체 관리 |

화면에서 버튼만 숨기는 것으로 끝내지 않고 Supabase RLS와 RPC 안에서도 같은 역할을 확인해야 한다. 서비스 역할 키는 브라우저에 노출하지 않는다.

## 8. 데이터 API 설계

브라우저에서 테이블별 count 요청을 여러 번 보내지 않고 운영용 RPC로 합친다.

- `admin_dashboard_stats()`: 건수, 데이터 최신 시각, 최근 파이프라인 상태
- `admin_search_members(...)`: 선수 검색·필터·페이지네이션
- `admin_search_matches(...)`: 경기 검색·필터·페이지네이션
- `admin_ranking_review(...)`: 티어 괴리와 근거 데이터
- `admin_save_match(...)`: 경기와 세트 원자적 저장
- `admin_change_log(...)`: 변경 이력 조회

현재 `admin_dashboard_stats()`와 경기 서버 페이지네이션은 구현되어 있다. 다음 우선순위는 선수 검색 RPC와 변경 이력이다.

## 9. 프런트엔드 코드 구성

현재 `page-admin.js` 한 파일에 기능이 집중되어 있다. 다음 단위로 분리한다.

```text
admin/
  core.js          인증, 권한, 상태 메시지, 공통 요청
  dashboard.js     운영 현황
  members.js       선수·팀
  matches.js       경기·세트
  ranking.js       랭킹·괴리 검토
  content.js       일정·연혁·내비게이션
  videos.js        채널·영상
  settings.js      설정·권한
  audit.js         변경 이력
```

각 모듈은 `load`, `render`, `edit`, `save` 책임을 갖고 공통 인증과 오류 처리는 `core.js`에서 담당한다.

## 10. 구현 순서

### 1단계 — 운영 가시성

- 통합 현황 RPC 적용
- KST 시각 통일
- 파이프라인 지연·실패 표시
- 저장 항목별 반영 시점 배지

### 2단계 — 큰 목록 개선

- 선수·경기·영상 서버 검색
- 커서 또는 범위 페이지네이션
- URL에 검색·페이지 상태 저장
- 목록 캐시와 중복 요청 제거

### 3단계 — 랭킹 검토

- 괴리 근거 화면
- 승인·보류·기각 상태
- 승인 이력
- 재계산 요청과 결과 연결

### 4단계 — 권한·감사

- 역할별 메뉴와 쓰기 정책
- 변경 전후 값 기록
- 대량 변경 미리보기
- 위험 작업 재인증

### 5단계 — 코드 분리

- 도메인별 JavaScript 모듈화
- 공통 폼·목록·페이지네이션 부품화
- 관리자 주요 흐름 브라우저 테스트

## 11. 완료 기준

- 관리자가 첫 화면에서 10초 안에 데이터 지연과 실패 여부를 판단할 수 있다.
- 모든 저장 작업에서 공개 반영 시점을 알 수 있다.
- 선수·경기 목록 크기가 늘어도 첫 요청 행 수가 고정된다.
- 티어 변경 후보가 계산 근거와 함께 표시된다.
- 모든 쓰기 작업에 작업자와 변경 전후 값이 남는다.
- 외부 크론 실패와 웹 빌드 실패를 어드민에서 구분할 수 있다.
