# 티어 랭킹 모델 분석 및 개선 내역

작성일: 2026-09-23

## 모델 구조

현재 모델은 단순 Elo 누적이 아니라 가중 Bradley–Terry 로지스틱 모델이다.

`선수 실력 = 티어 기준선 + 티어 내 개인 편차`

- 티어 기준선은 티어 간 경기 연결을 오래 보존하기 위해 540일 반감기를 사용한다.
- 개인 편차는 최근 폼을 반영하기 위해 90일 반감기를 사용한다.
- 개인·대학 대회는 1.0, 대학대전·미니는 0.9, 프로리그·CK는 0.8, 스폰은 0.6 가중치를 사용한다.
- 순위 점수는 적합 레이팅에서 표준오차의 1.5배를 뺀 보수 추정값이다.
- 최근 365일 10경기 미만 선수와 휴면 선수는 순위에서 제외한다.

이 구조는 경기망이 카드 티어와 숫자 티어로 거의 분리된 데이터에서 일반 Elo보다 안정적이다. 동일 티어 내 순위와 전체 티어 간격을 서로 다른 시간 창으로 계산하는 것도 현재 데이터 특성에 맞는다.

## 확인한 문제

1. 최근 18개월 월별 이력을 실행마다 전부 다시 적합해 전체 약 255초가 걸렸다.
2. 현재 월 이력은 직전에 계산한 현재 랭킹과 같은 입력인데도 다시 계산했다.
3. 데이터 티어는 원시 티어 기준선 중 가장 가까운 값을 골랐다. 연결이 약한 경계에서 기준선이 근소하게 뒤집히거나 표본이 적은 선수의 점 추정치가 흔들리면 과도한 괴리가 표시될 수 있었다.
4. 과거 경기나 승급 기록이 수정됐을 때와 단순히 현재 월 경기가 추가됐을 때를 구분하지 않아 안전한 캐시를 사용할 수 없었다.

## 적용한 개선

### 증분 월별 이력

- 마감 월 경기와 승급 이력, 경기 범주, 계산 버전을 SHA-256 지문으로 저장한다.
- 지문이 같으면 활성 스냅샷의 마감 월 결과를 재사용한다.
- 현재 월 이력은 이미 계산한 현재 랭킹 결과를 공유한다.
- 과거 경기·승급 기록·범주 또는 계산 버전이 바뀌면 자동으로 전체 재계산한다.
- 월이 바뀌면 직전 월이 닫힌 데이터에 포함되어 지문이 달라지므로 한 번 전체 검증 계산한다.

평상시 경로에서는 H2H와 현재 랭킹만 계산하므로 기존 측정 기준 약 255초에서 약 40.5초 수준으로 줄어들 수 있다. 실제 GitHub Actions 시간은 Supabase 조회·업로드 시간을 추가로 포함한다.

### 불확실성을 반영한 티어 괴리

- 티어 기준선은 가중 PAVA로 강한 티어부터 단조 감소하도록 보정한 뒤 괴리 경계에 사용한다.
- 선수 점 추정치 하나가 아니라 `레이팅 ± 1.5 × 표준오차` 구간을 사용한다.
- 이 구간 전체가 인접 티어의 중간 경계를 넘어야만 괴리로 표시한다.
- 한 번에 최대 두 단계까지만 제안한다.
- 실제 티어 내 순위와 원래 적합 레이팅은 바꾸지 않는다. 괴리 표시는 관리자 검토 신호로만 사용한다.

이 방식은 경기 수가 적은 선수의 과도한 승급·강등 신호와 연결망 경계의 미세한 역전에서 생기는 오탐을 줄인다.

## 검증 기준

- 계산식 단위 테스트: 단조 기준선, 불확실성 경계, 최대 이동 폭
- 캐시 단위 테스트: 현재 월 변경은 재사용, 마감 월 변경은 무효화
- 월별 이력 테스트: 캐시가 있으면 별도 월 적합을 실행하지 않음
- 운영 검증: 전체 선수 순위, 티어별 상위권, 승급 직전 선수 포착률, 미래 90일 로그손실을 이전 스냅샷과 비교

운영 데이터에서 전체 재계산한 첫 스냅샷을 기준선으로 보관하고, 이후 증분 스냅샷과 월별 값이 동일한지 한 달간 비교하면 캐시 안전성까지 확인할 수 있다.
