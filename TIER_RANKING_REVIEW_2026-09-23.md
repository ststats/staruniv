# 티어 랭킹 계산식 분석 및 개선안

작성일: 2026-09-23  
대상: `ststat/processors/staruniv_ranking.py`

## 1. 결론

현재 계산식은 일반 Elo보다 이 데이터에 더 적합하다. 경기망이 카드 티어와 숫자 티어로 거의 분리되어 있으므로 단일 Elo를 그대로 적용하면 두 집단의 절대 위치가 틀어지기 때문이다. 현재 모델은 `티어 기준선 + 선수 개인 편차`를 따로 적합하고, 각 부분에 서로 다른 반감기를 적용해 이 문제를 완화한다.

다만 현재 순위를 바로 교체할 정도의 큰 수식 변경은 권장하지 않는다. 다음 개선의 핵심은 가중치를 임의로 더 조정하는 것이 아니라 **시계열 검증 체계를 먼저 고정하고, 반복 경기의 상관관계와 티어 경계 제약을 모델 안에 넣는 것**이다.

권장 순서는 다음과 같다.

1. 시계열 백테스트와 보정 지표를 자동화한다.
2. 같은 날 같은 선수 조합의 반복 경기에 가중치 상한을 둔다.
3. 티어 기준선의 순서를 최적화 과정에서 직접 제약한다.
4. 표준오차 계산을 공분산까지 반영하는 방식으로 개선한다.
5. 위 변경을 각각 독립적으로 검증한 뒤 성능이 좋아진 조합만 운영 모델에 반영한다.

## 2. 현재 계산식

선수 `i`의 실력은 다음처럼 표현한다.

```text
θᵢ = m[tierᵢ] + δᵢ
```

- `m[tierᵢ]`: 해당 티어의 기준선
- `δᵢ`: 같은 티어 안에서 선수 개인의 편차

선수 `i`가 선수 `j`를 이길 확률은 Bradley–Terry 로지스틱 식이다.

```text
P(i > j) = sigmoid(θᵢ - θⱼ)
```

최적화 목적함수는 가중 로그손실과 개인 편차 prior를 합친 형태다.

```text
Loss = Σ wₖ · log(1 + exp(-(θwinner - θloser)))
       + 1/2 · Σ λᵢδᵢ²
       + λm/2 · Σ mtier²
```

경기 가중치는 경기 형식과 경기 경과 시간을 곱해 계산한다.

```text
w = category_weight × 0.5^(경과일 / 반감기)
```

현재 주요 값은 다음과 같다.

| 항목 | 현재 값 | 목적 |
|---|---:|---|
| 개인 폼 반감기 | 90일 | 최근 경기력 반영 |
| 티어 기준선 반감기 | 540일 | 티어 집단 사이의 약한 연결 보존 |
| 개인·대학 대회 | 1.0 | 중요 경기 |
| 대학대전·미니 | 0.9 | 준중요 경기 |
| 프로리그·CK | 0.8 | 중간 가중치 |
| 스폰·기타 | 0.6 | 많은 반복 경기의 영향 완화 |
| 개인 편차 prior σ | 0.675 | 저표본 선수 수축 |
| 미분류 prior σ | 3.0 | 미분류 선수를 특정 티어에 고정하지 않음 |
| 최근 경기 자격 | 365일 10경기 | 사실상 활동하지 않는 선수 제외 |

티어 기준선은 540일 창으로 먼저 적합하고, 그 기준선을 고정한 뒤 개인 편차를 90일 창으로 적합한다. 최종 티어 내 순위는 점 추정치가 아니라 다음 보수 점수를 사용한다.

```text
rank_score = θ - 1.5 × standard_error
```

표본이 적은 선수는 불확실성만큼 아래로 조정되므로 몇 경기만 이긴 선수가 바로 상위권에 오르는 현상을 줄인다.

## 3. 현재 모델의 장점

### 분리된 경기망을 다룬다

카드 티어와 숫자 티어 사이 직접 경기가 매우 적다. 현재 모델은 티어 기준선을 장기 데이터로 적합해 두 집단을 연결하므로 일반 Elo에서 발생하는 절대 위치 왜곡을 줄인다.

### 최근 폼과 티어 구조를 분리한다

개인 폼은 빠르게 바뀌지만 티어 간 평균 격차는 상대적으로 천천히 바뀐다. 두 반감기를 분리한 것은 데이터 생성 구조에 맞는 선택이다.

### 저표본 선수를 보수적으로 다룬다

개인 편차 prior와 표준오차 차감을 함께 사용한다. 단순 승률이나 점 추정치 순위보다 작은 표본에 강하다.

### 운영 안정성이 확보되어 있다

월별 이력은 입력 지문이 같으면 마감 월을 재사용하고, 과거 경기·승급 정보·계산 버전이 바뀌면 전체 재계산한다. 평상시 빌드 병목은 기존 약 255초에서 약 40.5초 수준으로 줄일 수 있다.

## 4. 남은 문제

### P0 — 모델 변경을 판정하는 공통 기준이 부족하다

코드 주석에 여러 홀드아웃 결과가 기록되어 있지만, 모든 후보식을 같은 데이터 분할과 지표로 반복 실행하는 자동 평가기가 없다. 이 상태에서는 한 지표를 개선한 변경이 순위 안정성이나 티어 경계를 악화시킬 수 있다.

필요한 지표:

- 미래 30·90·180일 경기 로그손실
- Brier score
- 예측확률 구간별 실제 승률을 보는 calibration error
- 이전 스냅샷 대비 티어 내 순위 변동량
- 최근 실제 승급자의 승급 전 상위 10%·25% 포착률
- 경기 수 구간별 상위권 진입률
- 카드/숫자 티어 경계의 추정 오차

### P1 — 반복 경기를 독립 표본으로 본다

같은 날 같은 두 선수가 여러 번 경기하면 각 경기를 독립 정보로 더한다. 그러나 같은 컨디션·같은 매치 환경에서 나온 반복 경기는 완전히 독립적이지 않다. 반복 수가 많은 스폰이나 연속 세트가 추정 정밀도를 실제보다 높게 만들 수 있다.

개선안:

```text
같은 날짜 + 같은 선수쌍 + 같은 경기 형식의 합산 가중치
= min(원래 합산 가중치, base_weight × sqrt(경기 수))
```

원본 승패는 보존하면서 반복 횟수가 정밀도를 선형으로 부풀리는 문제를 줄일 수 있다. `sqrt`, `log1p`, 상한 고정 방식을 시계열 백테스트로 비교해야 한다.

### P1 — 티어 기준선 순서가 목적함수 안에서 보장되지 않는다

현재는 괴리 표시 단계에서 PAVA로 기준선을 단조 보정한다. 실제 랭킹 적합값은 원시 기준선을 사용하므로 연결 경기가 매우 적은 경계에서 기준선이 뒤집힐 가능성은 남아 있다.

개선안은 최적화 변수 자체를 순서가 보장되는 형태로 바꾸는 것이다.

```text
m₀ = 자유 변수
m₁ = m₀ - softplus(gap₁)
m₂ = m₁ - softplus(gap₂)
...
```

이렇게 하면 `갓 ≥ 킹 ≥ 잭 ≥ ... ≥ 베이비`가 항상 성립한다. 다만 실제 데이터가 일시적으로 반대 신호를 낼 때 손실이 얼마나 늘어나는지 검증해야 한다.

### P1 — 현재 표준오차는 선수별 대각 근사다

현재 정밀도는 해당 선수 경기의 `w·p(1-p)`를 합산한다. 계산이 빠르고 방향은 맞지만 선수들 사이 공분산, 특히 연결이 약한 두 경기망의 공동 불확실성을 반영하지 못한다.

개선안:

- 최적점에서 sparse Hessian을 만든다.
- 전체 역행렬 대신 필요한 대각 성분을 sparse solve로 계산한다.
- 카드/숫자 경계를 잇는 방향의 불확실성을 별도 진단값으로 제공한다.

이 개선은 순위 점수와 티어 괴리 신뢰도를 동시에 높일 수 있다.

### P2 — 경기 형식 가중치가 전 선수에게 동일하다

현재 범주 가중치는 전체 평균으로 정한다. 선수마다 스폰과 대회의 성격이 다를 수 있지만 선수별 가중치를 도입하면 과적합 위험이 크다. 우선 전체 가중치를 rolling validation으로 자동 선택하고, 필요하면 티어 그룹별 가중치까지만 검토하는 것이 안전하다.

### P2 — 승급일 데이터가 불완전하면 과거 티어를 정확히 복원할 수 없다

승급 사다리가 역행하거나 현재 티어와 마지막 승급 기록이 맞지 않는 선수는 현재 티어를 사용한다. 계산식보다 원본 데이터 품질 문제다.

관리 화면에서 다음 항목을 별도 검증 대기 목록으로 제공해야 한다.

- 승급 순서가 역행한 선수
- 마지막 승급 티어와 현재 티어가 다른 선수
- 승급일이 없는데 티어가 바뀐 선수
- 동일 날짜에 여러 승급 티어가 있는 선수

## 5. 티어 괴리 판정

현재 개선된 괴리 판정은 실제 티어를 바로 바꾸는 자동 판정이 아니다. 관리자에게 검토할 선수를 알려주는 신호다.

적용된 규칙:

1. 티어 기준선을 강한 순서로 단조 보정한다.
2. 선수의 `θ ± 1.5×표준오차` 구간을 계산한다.
3. 구간 전체가 인접 티어 중간 경계를 넘어야 한 단계 이동한다.
4. 최대 두 단계까지만 제안한다.
5. 실제 티어 내 순위는 기존 보수 점수로 유지한다.

추가로 괴리 목록에는 다음 근거를 함께 표시하는 것이 좋다.

- 최근 30·90·365일 경기 수와 승률
- 현재 티어 내 순위와 표본 수준
- 제안 티어 및 넘은 경계
- 신뢰구간
- 주요 상대 티어별 전적
- 최근 티어 변경일

## 6. 권장 차기 모델 실험

| 순서 | 실험 | 채택 조건 |
|---|---|---|
| 1 | 현재 모델을 자동 백테스트 기준선으로 고정 | 재현 가능한 기준 결과 생성 |
| 2 | 날짜·선수쌍 반복 가중치 `sqrt(n)` | 로그손실 또는 calibration 개선, 순위 급변 증가 없음 |
| 3 | 티어 기준선 순서 제약 | 경계 안정성 개선, 전체 로그손실 악화가 허용 범위 이내 |
| 4 | sparse Hessian 표준오차 | 저표본·약연결 선수 괴리 오탐 감소 |
| 5 | 범주 가중치 자동 탐색 | 여러 시점 평균에서 지속적으로 개선 |

한 번에 여러 변경을 합치지 말고 각 실험을 독립적으로 비교해야 어느 요소가 실제로 개선했는지 판단할 수 있다.

## 7. 운영 권고

- 평상시 외부 크론은 증분 계산을 사용한다.
- 매월 첫 실행에는 닫힌 월을 포함해 전체 월별 이력을 다시 검증한다.
- 계산 버전을 스냅샷 메타데이터에 남긴다.
- 모델 변경 전후 스냅샷을 최소 3개 보관한다.
- 티어 괴리는 자동 반영하지 않고 관리자 승인 대상으로 유지한다.
- 백테스트가 기준 모델보다 나빠지면 배포를 중단하도록 CI에 연결한다.
