{
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- .",
  "routes": [
    { "src": "^/((?:[^/]+/)*[^/.]+)$", "status": 308, "headers": { "Location": "/$1/" } },
    { "src": "^/(.*)$", "dest": "https://ststats.github.io/staruniv/$1" }
  ]
}
