# STSTATS

스타 방송을 하는 스트리머들의 통계를 자동으로 모아서 보여주는 프로젝트입니다.
2026년 9월부터, `data/members.xlsx`에 등록된 스트리머 전원(활동 팀 소속 + FA/휴면 포함)의

- **별풍선 / 방송시간 / 누적시청자** — 풍고(poonggo.com) 공식 API에서 자동 수집
- **스폰전적(스폰판수)** — EloBoard(eloboard.co.kr) 공식 API에서 자동 수집

를 모아서, 세 종류의 페이지로 보여줍니다. 날짜/지표 드롭다운으로 즉시 전환되며, 페이지 이동
(새로고침)이 없습니다. 화면에 그리는 작업은 전부 브라우저 자바스크립트가 하고(클라이언트
렌더링), 파이썬 스크립트는 매일 최신 데이터를 JSON으로만 만들어둡니다.

## 페이지 3종류

| 페이지 | 경로 | 내용 |
|---|---|---|
| 전체 페이지 | `docs/index.html` | 모든 팀 비교 그리드 + FA 명단 |
| 팀별 페이지 | `docs/team.html?team=팀이름` | 팀 하나만 떼서 보는 화면 |
| 개인 프로필 | `docs/profile.html?id=SOOP아이디` | 파일 하나로 전 인원 대응 - 성별/생년월일/소속/직책/종족/티어/방송국/지표 4개 |

## 폴더 구조

```
ststats/
├── requirements.txt          ← 파이썬 의존성 전체(Pillow + requests + openpyxl + fonttools[woff] + jinja2) - 레포 루트에 있어야 pip 캐시가 동작함
├── data/
│   ├── members.xlsx                     ← 로스터의 유일한 원본(source of truth). admin.html도
│   │                                        이제 이 파일을 직접 읽고 쓴다(SheetJS로 브라우저에서
│   │                                        바로 파싱)
│   ├── members.json                     ← (자동 생성) members.xlsx로부터 매번 새로 만들어지는
│   │                                        파생 결과물 - 나머지 파이프라인(update_data.py,
│   │                                        generate_pages.py 등)은 이 파일을 읽는다
│   ├── archive_corrections_applied.json ← (자동 생성) 소급 정정 내부 상태 - 아래 참고
│   ├── latest.json          ← 오늘 수집 결과 (자동 생성)
│   └── archive/
│       └── YYYY-MM-DD.json  ← 지난 날짜들의 스냅샷 (자동 생성, 그 당시 팀 구성 그대로 보존)
├── scripts/
│   ├── _common.py                  ← ROOT 경로, 시간/숫자 헬퍼, 공용 HTTP 재시도 헬퍼(fetch_json) 등
│   │                                   여러 스크립트가 같이 쓰는 유틸리티 모음
│   ├── subset_font.py               ← Pretendard 전체 폰트를 다운로드해서, 실제 로스터+아카이브+
│   │                                   생성된 HTML에서 쓰이는 문자만 추려 훨씬 작은 서브셋
│   │                                   (docs/fonts/PretendardVariable.woff2)으로 만든다.
│   │                                   generate_pages.py 다음에 실행돼야 한다(생성된 HTML을
│   │                                   스캔하기 때문)
│   ├── convert_members_xlsx.py     ← members.xlsx(유일한 원본)로부터 members.json을 매번
│   │                                   새로 만들어내는 단방향 변환(더 이상 병합이 아님)
│   ├── sync_members.py             ← EloBoard 티어 목록 API에서 신규 멤버 자동 추가/일부 필드 갱신
│   │                                   (members.xlsx를 직접 읽고 씀)
│   ├── fetch_poonggo_data.py       ← 풍고 API에서 별풍선/방송시간/누적시청자를 "가져오기만" 하는 순수 함수
│   │                                   (언제 저장할지, 아카이브를 어떻게 할지는 전혀 모름)
│   ├── fetch_eloboard_data.py      ← EloBoard API에서 스폰전적을 "가져오기만" 하는 순수 함수
│   │                                   (파일을 직접 안 씀 - 신규 elo_id 등록은 update_data.py가 담당,
│   │                                   아래 "신규 elo_id 자동 등록" 참고)
│   ├── update_data.py              ← 위 둘을 호출해서 데이터를 합치고, 아카이빙·월 확정(별풍선+
│   │                                   스폰전적 둘 다)·소급 정정·신규 elo_id 등록·latest.json 저장까지
│   │                                   전부 조율하는 오케스트레이터 - 실제 데이터 갱신은 이 스크립트
│   │                                   하나로 끝남
│   ├── generate_pages.py           ← data/*.json을 docs/data/daily/로 복사하고, 전체/팀별/프로필 뼈대 HTML 생성.
│   │                                   실제 CSS/JS/HTML은 아래 templates/ 폴더에 있고, 이 스크립트는
│   │                                   Jinja2로 그 파일들을 읽어서 조립만 한다
│   └── templates/
│       ├── page.html.j2                ← HTML 뼈대(head/body). style.css/mobile.css/app.js.j2를
│       │                                    변수로 받거나 include해서 조립한다
│       ├── style.css                    ← 전체 CSS (PAGE_CSS였던 것)
│       ├── mobile.css                   ← 모바일 미디어쿼리 CSS (MOBILE_CSS였던 것 - 전체/팀 페이지에만
│       │                                    포함되고, 프로필/특정 팀 페이지엔 안 들어감)
│       └── app.js.j2                    ← 클라이언트 JS 전체(달력, 정렬, 라이브 표시, 렌더링 등)
├── docs/
│   ├── index.html           ← 전체 페이지 (GitHub Pages가 이 폴더를 서빙)
│   ├── admin.html            ← 멤버 관리 어드민 페이지 (GitHub API로 직접 커밋)
│   ├── team.html             ← 팀별 페이지 (?team=팀이름 으로 전 팀 대응, 파일 하나)
│   ├── profile.html          ← 개인 프로필 페이지
│   ├── data/
│   │   ├── dates.js              ← 날짜 선택 UI용 전체 날짜 목록만 담은 작은 파일
│   │   │                            (아래 "내부 동기화 상태 파일" 참고)
│   │   └── daily/
│   │       └── YYYY-MM-DD.json  ← 브라우저가 날짜 선택시 fetch()로 불러오는 원본 데이터
│   │                              (data/latest.json + data/archive/*.json을 그대로 복사한 것,
│   │                               매 실행마다 통째로 새로 채움 - 원본이 지워지면 여기도 같이 지워짐)
│   ├── fonts/
│   │   └── PretendardVariable.woff2  ← self-host 폰트 (매일 실제 쓰는 문자만 서브셋해서 생성)
│   └── logos/
│       └── {팀이름}.webp    ← 팀 로고 (있으면 자동 표시, 없으면 텍스트만)
└── .github/workflows/
    └── update-stats.yml       ← 데이터 갱신 전체(멤버 동기화 + 풍고+엘로보드 + 페이지 생성 +
                                  폰트 서브셋) - 수동 버튼 또는 외부 스케줄러가
                                  workflow_dispatch로 트리거
```

## 데이터 소스 두 곳

1. **풍고(poonggo.com)** — 정식 이용 허가를 받아 사용 중인 공식 API (`/api/monthly`). 별풍선/방송시간/누적시청자를 API 호출 1번으로 모두 얻습니다. `fetch_poonggo_data.py`가 담당합니다.
2. **EloBoard(eloboard.co.kr)** — 스폰전적(승/패). 공식 API(`/api/matches`)를 `player_id`(=`members.xlsx`의 ELO ID) 기준으로 페이지네이션하며 조회합니다. `fetch_eloboard_data.py`가 담당합니다.

두 스크립트 모두 HTTP 요청은 `_common.fetch_json()`을 거칩니다 - 타임아웃(30초)과 재시도(최대 3회, 3초 간격 백오프)가 여기 한 곳에서 통일되어 있어서, 일시적인 네트워크 오류 하나 때문에 그 실행에서 수집한 데이터가 조용히 반쪽만 저장되는 일이 없습니다(재시도를 다 소진하면 예외 대신 실패를 명확히 알리고, 호출부는 기존 값을 그대로 유지합니다).

`fetch_poonggo_data.py`와 `fetch_eloboard_data.py`는 **각자 데이터를 가져오기만 하는
순수 함수**입니다 - 파일을 직접 쓰지 않고, `data/latest.json`을 언제 쓸지, 아카이브를
언제 만들지, 신규 elo_id를 등록할지는 전혀 모릅니다. 그 조율은 전부 `update_data.py`
하나가 담당합니다:

1. 날짜가 바뀌었으면 어제자 스냅샷을 아카이브로 확정
2. 달이 바뀌었으면 지난달 마지막 날 아카이브에 별풍선(풍고 재조회)과 스폰전적
   (엘로보드 재조회)을 **둘 다** 확정 적용
3. `info_updated_at` 소급 정정 적용
4. 오늘자 별풍선/스폰전적을 새로 가져와서 하나로 합쳐 `latest.json`에 저장

원래는 두 스크립트가 각자 독립적으로 `latest.json`을 읽고 쓰다 보니, 어느 스크립트가
먼저 도는지에 따라 "아카이브 파일이 아직 없어서 확정을 못 함" 같은 순서에 민감한
버그가 계속 생겼습니다. 지금은 한 곳에서만 판단하므로 그런 문제 자체가 생길 수
없습니다. 엘로보드 쪽 API 호출이 실패해도(재시도까지 다 소진한 경우) `update_data.py`가
내부적으로 예외를 잡아 기존 스폰전적을 보존하고 별풍선 데이터는 정상적으로 저장합니다
- 워크플로우 자체가 실패하지 않습니다.

## 하루 단위로 데이터가 쌓입니다

`update_data.py`는 실행될 때마다 오늘 날짜 기준으로 `data/latest.json`을 갱신하고,
**날짜가 바뀐 걸 감지하면** 어제자 스냅샷을 `data/archive/YYYY-MM-DD.json`으로 보관합니다.
**성별/생일**은 안 바뀌는 정보라 이 스냅샷들엔 아예 안 들어가고 `members.xlsx`에서만
관리되며, 페이지를 그릴 때 그때그때 조회해서 씁니다.

**달이 바뀌는 시점**(예: 8월 마지막 날 → 9월 1일 첫 실행)에는, 그냥 복사만 하지 않고
**별풍선(풍고 API)과 스폰전적(엘로보드 기간검색) 둘 다 8월 기준으로 한 번 더 재조회**해
확정된 값으로 갱신한 뒤 그 날짜로 보관합니다(두 데이터 모두 월말 이후 뒤늦게 갱신될
가능성에 대비 - 예를 들어 8월 마지막 날 자정 직전에 발생한 스폰전적도, 재조회 시점엔
eloboard.com에 반영돼있을 테니 놓치지 않고 잡힙니다). 이 확정은 달이 바뀐 **첫 실행에서
바로** 일어납니다 - 아카이빙과 월 확정이 같은 스크립트 안에서 순서대로 처리되므로,
"아카이브 파일이 아직 없어서 확정을 못 하고 다음 실행까지 미뤄지는" 상황이 생기지
않습니다.

## 팀/직책/종족/티어를 나중에 고치면 - `info_updated_at`

`members.xlsx`(admin.html도 마찬가지)에서 어떤 사람의 team/role/race/tier/nickname을
고치고 "수정일"에 **오늘 날짜("YYYY-MM-DD")**를 넣으면, 다음 자동 실행 때
**그 날짜 이후의 모든 일별 아카이브**에 새 값이 자동으로 소급 적용됩니다. 예를 들어
어떤 사람이 사실 지난주부터 다른 팀이었다는 걸 뒤늦게 알았다면, 수정일을
그 날짜로 넣어두면 이미 만들어진 지난 며칠치 기록까지 알아서 고쳐집니다.

소급 적용이 성공적으로 끝나면 "수정일" 칸은 **자동으로 비워집니다** - 그러니 다음에
이 사람의 다른 정보를 또 고칠 땐 매번 새 날짜를 명시적으로 넣어야만 소급 적용이
걸립니다(수정일을 안 넣으면 그 변경은 오늘부터만 반영되고 과거 아카이브는 안 건드립니다).

## 팀별 순위와 전달 대비 변동

각 팀은 지표별 평균 기준으로 정렬되고(스폰판수만 여자 평균 기준, 나머지는 전체 평균 기준),
팀 이름 옆에 전달 대비 순위 변동이 표시됩니다(▲상승 / ▼하락 / - 동일 / NEW 신규 - 전체
페이지에서만, 팀 단독 페이지엔 표시 안 함). "전달"은 지금 보고 있는 날짜의 전달 중
**가장 최근 날짜**를 기준으로 비교합니다(일별 아카이브라 특정 하루를 콕 집어 비교할 수
없어서, 그 달의 마지막 스냅샷 = 최종 확정치를 씁니다). 비교할 전달 데이터가 없으면
표시되지 않습니다.

## FA 명단 (전체 페이지 하단)

`team`이 `FA`인 사람들을 모아서 전체 페이지 맨 아래 카드에 보여줍니다(팀 단독 페이지엔
안 나옵니다). 기본은 접힌 상태로 시작하고, 눌러서 펼치면 사진+닉네임이 티어 순서
(갓 → 킹 → 잭 → 조커 → 스페이드 → 0~8 → 유스)로 정렬돼 나옵니다. 목록에 없는
티어 값(빈 값 등)은 맨 뒤로 갑니다.

## 팀 로고 (`docs/logos/`)

`docs/logos/{팀이름}.webp` 파일이 있으면 표 상단 팀 이름 옆에 자동으로 로고가 붙고, 그
로고 색상을 뽑아 팀 카드/개인 프로필 상단 바 색으로도 씁니다. 파일명은 `members.xlsx`의
"소속" 값과 정확히 일치해야 합니다. 파일이 없는 팀은 그냥 텍스트만 표시되며 오류가 나지
않습니다. `FA`/`휴면`은 애초에 팀이 아니라서, 개인 프로필 상단 바가 자동으로 회색 고정됩니다.

## 모바일 화면

전체 페이지(팀 비교 그리드)는 좁은 화면에서 2열로 압축되면서 글자 크기 등이 줄어듭니다.
반대로 **개인 프로필/팀 단독 페이지는 원래부터 1열 레이아웃이라 좁은 화면에서도 공간
다툼이 없어서, 데스크톱 크기 그대로 유지**됩니다(억지로 줄이면 오히려 안 읽혀서).

## 멤버 관리 어드민 페이지 (`docs/admin.html`)

`data/members.xlsx`를 엑셀 프로그램으로 직접 여는 대신, 브라우저에서 표로 보면서
검색·추가·수정·삭제하고 바로 커밋까지 할 수 있는 페이지입니다(SheetJS로 xlsx를
브라우저에서 직접 읽고/쓰며, 다른 시트나 서식은 안 건드리고 데이터 부분만 갈아끼웁니다).

1. `https://ststats.github.io/staruniv/admin.html` 접속
2. **"repo" 권한만 있는** GitHub Personal Access Token 발급받아 맨 위 칸에 입력 → **토큰 저장**
   (이 탭에서만 기억되고, 레포에는 저장되지 않습니다)
3. **불러오기** → 검색으로 찾거나 표에서 셀 직접 수정(아이디/엘로ID/닉네임/성별/생일/
   직책/팀/종족/티어/수정일) / **+ 새 멤버 추가** / 행별 **삭제**
4. **저장** → `members.xlsx`만 커밋하고 끝(워크플로우는 안 돌림, 여러 건 나눠서
   수정할 때 유용)
5. **저장 + 워크플로우 실행** → `members.xlsx`가 커밋되고 `update-stats.yml`이
   즉시 트리거되어 몇 분 안에 사이트에 반영됩니다.

`OWNER`/`REPO`/`BRANCH` 값은 `admin.html` 코드 맨 위에 상수로 있습니다 - 저장소를 옮기거나
기본 브랜치가 `main`이 아니면 그 값만 고치면 됩니다.

## 멤버 관리 (인원이 바뀔 때)

`data/members.xlsx`가 로스터의 유일한 원본입니다. 엑셀 파일을 직접 열어서 고치거나
`docs/admin.html`(브라우저에서 GitHub에 바로 커밋)로 고치면 됩니다. 시트 이름은
`members`, 컬럼 순서는 다음과 같습니다:

| 컬럼 | 의미 |
|---|---|
| 이름 | 표시할 닉네임 |
| SOOP ID | SOOP(구 아프리카TV) 아이디 - 풍고 조회용 |
| ELO ID | EloBoard 사이트의 player_id(숫자) - 스폰전적 조회용, 모르면 빈 칸 |
| 생년월일 | YYYY-MM-DD, 모르면 빈 칸 - 이름 옆 🎂 표시에 쓰임 |
| 성별 | 남자 / 여자 |
| 종족 | 테란/프로토스/저그, 모르면 빈 칸 |
| 티어 | 갓/킹/잭/조커/스페이드/0~8/유스, 모르면 빈 칸 |
| 소속 | 팀 이름 (또는 FA / 휴면) |
| 직책 | 수장 / 전력외 (없으면 빈 칸) |
| 수정일 | YYYY-MM-DD - 아래 "팀/직책/종족/티어를 나중에 고치면" 참고 |

- **ELO ID**가 비어있으면 스폰전적 매칭 대상에서 자연히 빠지고, 별풍선/방송시간/누적시청자는
  정상 수집됩니다.
- **직책**이 `수장` 또는 `전력외`인 사람은 표에서 빨간 배경으로 표시되고, 별풍선/방송시간/누적시청자/스폰판수
  집계에서 모두 제외됩니다.
- **소속**이 `FA` 또는 `휴면`인 사람은 데이터(풍고/ELO)는 똑같이 수집되지만, 팀 카드/팀 페이지
  집계·표시 대상에선 제외됩니다(`FA`는 대신 전체 페이지 하단 FA 명단에 나옵니다). 개인 프로필
  페이지에서는 정상 조회됩니다.
- 새로 추가된 사람/팀은 전체 페이지·팀 페이지에 **자동으로** 반영됩니다.
- 아직 확인 못 한 값이 있으면 빈 칸으로 두는 걸 권장합니다. `"체크"`, `"TODO"`, `"?"`, `"미정"`
  같은 임시 표시 문자열을 넣어도 사이트에는 자동으로 "-"(빈 값)로 처리되어 안전하게 나가지만,
  나중에 실제 값으로 채워 넣는 걸 잊기 쉬우니 가능하면 빈 칸을 쓰세요.
- 수정 후(직접 편집이든 admin.html이든) 다음 자동 실행(또는 수동 실행) 때부터 반영됩니다.


## 엘로보드 스폰전적 수집 방식

`fetch_eloboard_data.py`는 EloBoard 공식 API(`https://eloboard.co.kr/api/matches`)를
`limit`/`offset`으로 페이지네이션하며 호출합니다:

1. 한 페이지(최대 200건)를 가져와서, 각 경기(`match`)의 `played_on` 날짜를 조회 기간과 비교
2. 조회 기간보다 최신 경기는 건너뛰고, 기간 안에 들어오는 경기는 각 참가자(`participants`)의
   `player_id`(=`elo_id`)별로 판수/승/패를 누적 집계
3. 조회 기간보다 오래된 경기에 도달하면 더 볼 필요가 없으므로 그 자리에서 탐색 종료

API가 최신 → 과거 순으로 정렬해서 응답한다고 가정합니다. 페이지 요청이 실패하면
`_common.fetch_json()`이 자동으로 재시도하고, 그래도 실패하면 지금까지 모은 데이터가
불완전하다는 뜻이므로 부분 데이터를 저장하는 대신 빈 결과를 반환해 `update_data.py`가
기존 값을 유지하도록 합니다.

### 신규 elo_id 자동 등록

EloBoard에 이미 전적이 쌓이고 있는데 로스터엔 없는 사람이 있으면(예: elo_id는
있지만 SOOP 아이디는 아직 모르는 경우), `update_data.py`가 `미상(elo_{elo_id})`라는
임시 닉네임에 나머지 필드(성별/종족/티어/소속 등)를 빈칸으로 채운 "임시 프로필"을
`members.xlsx`에 자동으로 추가합니다(id는 `elo_{elo_id}` 형식의 가짜 SOOP 아이디).
`fetch_eloboard_data.py`는 순수하게 전적을 가져오기만 하고(파일을 직접 안 씀),
그 결과를 보고 신규 등록 여부를 판단해서 실제로 xlsx에 쓰는 건 `update_data.py`입니다.

자동 생성된 임시 프로필은 `id`가 진짜 SOOP 아이디가 아니라서(사진/방송 임베드 등은
그냥 조용히 안 뜸) 관리자가 나중에 admin.html에서 진짜 정보로 채워 넣어야 완전해집니다.

이 등록은 로스터가 **이미 메모리에 로드된 이후** 판단되므로, 그날 새로 발견된
사람의 별풍선/스폰전적은 당일에는 아직 안 잡히고 **다음 실행부터** 정상적으로
수집됩니다(다만 로스터 표시 자체는 같은 실행 안에서 `Sync members.xlsx ->
members.json (2차)` 스텝이 한 번 더 돌아서 그날 바로 반영됩니다 - 아래 워크플로우
설명 참고).

## 안정성 관련 안전장치

여러 소스(xlsx/EloBoard API/admin.html)가 같은 파일들을 건드릴 수 있는 구조라,
아래 안전장치들을 공용으로 넣어뒀습니다:

- **원자적 쓰기** (`_common.atomic_write_json`, `.xlsx` 저장용 동일 패턴) —
  `members.json`/`latest.json`/아카이브/`.xlsx` 전부 직접 쓰지 않고, 임시 파일에
  먼저 다 쓴 다음 `os.replace()`로 한 번에 바꿔치기합니다. 워크플로우가 쓰는
  도중에 죽어도(타임아웃, 강제취소 등) 원본 파일은 쓰기 시작 전 상태 그대로
  안전하게 남습니다.
- **안전한 읽기** (`_common.safe_read_json`) — 스냅샷 파일이 없거나 깨져있어도
  예외를 던지는 대신 기본값으로 대체하고 경고만 남깁니다. 실수로 지워지거나
  손상된 상태 파일 하나 때문에 전체 파이프라인이 계속 실패하는 일을 막습니다.
- **멤버 스키마 검증** (`_common.validate_and_clean_members`) — `id`/`nickname`이
  없는 레코드는 제외하고, `id`는 문자열로, `elo_id`는 정수로 강제 변환합니다.
  `convert_members_xlsx.py`가 xlsx를 json으로 바꿀 때, `update_data.py`가 그
  json을 읽을 때 각각 거쳐가서, 레코드 하나가 이상해도 나머지 전원의 그날
  데이터 갱신이 통째로 죽지 않습니다.
- **엘로보드 페이지네이션 상한선** (`fetch_eloboard_data.MAX_PAGES`) — API 정렬
  가정이 깨지는 등의 이유로 종료 조건에 영영 도달 못 하면, 무한정 도는 대신
  일정 페이지 수에서 실패로 처리하고 끝냅니다(그래야 워크플로우 전체 시간을
  다 잡아먹고 그날 아무 것도 못 하는 최악의 상황을 막습니다).
- **커밋/푸시 재시도** (`update-stats.yml`) — `members.xlsx`는 바이너리 파일이라
  워크플로우와 admin.html이 하필 비슷한 시점에 같이 커밋하면 git이 자동으로
  못 합칩니다(텍스트 파일과 달리 충돌로 처리됨). 그래서 push 실패 시 최대 5번,
  매번 실패 상태를 깨끗하게 정리(`rebase --abort`)하고 다시 pull+push를
  시도합니다. 그래도 다 실패하면 조용히 넘어가지 않고 워크플로우 자체를
  실패로 표시합니다(이번 실행분만 반영이 안 됐을 뿐, 다음 실행에서 다시
  시도됩니다).

## 내부 동기화 상태 파일

`data/` 안에 사람이 직접 안 건드리는 자동 생성 파일이 있습니다 - 지워도 다음 실행에서
다시 만들어지지만, 지우면 아래 설명한 최적화가 그 시점부터 다시 시작됩니다(데이터가
틀어지진 않고, 그냥 한 번 더 전체를 훑게 될 뿐입니다).

- **`data/archive_corrections_applied.json`** — `update_data.py`가 `info_updated_at`
  소급 정정을 아카이브에 반영할 때, "이 사람의 이 보정 내용은 이미 다 반영해뒀다"를
  기억해두는 파일입니다. 이게 없으면 소급 정정이 한 번이라도 걸린 사람이 생길 때마다
  매 실행마다 그 날짜 이후 아카이브 전체를 다시 훑게 되는데(아카이브는 계속 쌓이므로
  실행 시간이 계속 늘어남), 이 스냅샷 덕분에 실제로 새로 바뀐 보정 건에 대해서만
  다시 훑습니다. 더 이상 안 쓰이는 기록(그 사람의 수정일이 이미 비워진 경우)은
  일정 기간이 지나면 자동으로 정리되어, 이 파일 자체가 무한정 커지지 않습니다.
- **`docs/data/dates.js`** — 사이트가 브라우저에서 날짜 선택 UI를 그릴 때 쓰는
  전체 날짜 목록(`window.AVAILABLE_DATES = [...]`)만 담은 작은 파일입니다. 이걸
  `index.html`/`team.html`/`profile.html` 안에 직접 박아넣지 않고 `<script src>`로
  동기 로드하게 분리해뒀습니다 - 안 그러면 매일 날짜가 하나씩 늘어날 때마다 CSS/JS
  다 포함된 큰 HTML 3개(각 45~50KB급) 전체가 매일 다시 커밋돼서, 실제로 바뀐 것도
  없이 레포 용량만 계속 불어납니다. `_write_if_changed`가 이제 이 세 HTML 파일에도
  적용돼있어서, 로스터/디자인이 실제로 안 바뀐 날은 커밋 자체가 안 생깁니다.

## 수동으로 즉시 갱신하고 싶을 때

저장소 → **Actions** 탭 → **Update all stats** 워크플로우 선택 → **Run workflow** 버튼
(풍고+엘로보드 둘 다 이 워크플로우 하나로 갱신됩니다)

## 라이브 방송 표시 (이 저장소 밖에 있는 외부 인프라 의존)

전체/팀 페이지의 라이브 점 표시, 프로필 페이지의 방송 중 정보는 이 저장소
안의 GitHub Actions만으로는 안 돌아간다 - **Cloudflare Worker + Workers KV +
외부 크론 서비스(cron-job.org)** 조합으로 별도 운영되고 있다. 이 저장소를
클론해서 그대로 배포해도 이 기능만은 저절로 안 켜지고, 아래 인프라를 따로
셋업해야 한다:

1. **`live-status-worker.js`**를 Cloudflare Workers에 배포(대시보드에서 코드
   붙여넣고 Deploy)
2. Workers KV 네임스페이스를 만들어서 워커에 바인딩(변수 이름 `SOOP_KV`)
3. **cron-job.org**(또는 비슷한 외부 크론 서비스)에서 2분마다
   `https://{워커주소}/?cron_key={비밀키}`를 호출하도록 등록 - 이 호출이
   워커의 `runCronUpdate()`를 실행시켜서, SOOP 전체 라이브 목록을 훑어
   로스터와 대조한 결과를 KV에 저장한다
4. `generate_pages.py`의 `LIVE_STATUS_PROXY_URL` 상수가 그 워커 주소를
   가리켜야 한다

동작 원리: 방문자의 브라우저가 각자 SOOP에 개별 조회하는 대신(로스터 규모만큼
요청이 방문자 수에 비례해 계속 늘어나는 구조라 위험했음), 워커 하나가 대신
SOOP을 미리 훑어 KV에 캐시해두고 방문자는 그 캐시만 읽어간다. 이 방식을 채택하기
전에 GitHub Actions로 같은 작업(`fetch_live_status.py` / `update-live-status.yml`)을
대신 시도했었는데, 완전히 대체되어 저장소에서 삭제했다(git 히스토리에서는 조회 가능).

## 참고

- 풍고 데이터 출처: 풍고(poonggo.com) 공식 API (`/api/monthly`, ids 파라미터로 최대 300명씩 일괄 조회 가능,
  응답에 별풍선(`amt`)과 방송시간(`broadTime`, 초)이 함께 들어있어 API 호출 1번으로 두 지표를 모두 얻음 -
  사전에 이용 허가를 받아 사용 중)
- 별풍선/방송시간/누적시청자/스폰판수 모두 값이 0이거나 직책이 수장/전력외인 멤버는 팀 합계·평균·상위 1%/5%/10% 계산에서 제외됩니다.
- 실행 시각은 GitHub 자체 스케줄(`schedule:`)이 아니라 **외부 스케줄러(cron-job.org 등)가
  정확한 시각에 `workflow_dispatch`를 GitHub API로 직접 호출**하는 방식입니다. GitHub의
  자체 스케줄러는 "최선을 다하지만 정시 보장은 안 함"이라 정시성이 중요하면 믿을 수
  없어서(공식 문서에도 명시), 이 방식으로 대체했습니다.
- `update_data.py`는 실행되는 순간의 실제 KST를 기준으로 날짜를 판단하므로, 실행이
  몇 분~몇십 분 밀려도 안전합니다(통째로 하루를 건너뛸 정도로 심하게 밀리는 경우만 예외).
