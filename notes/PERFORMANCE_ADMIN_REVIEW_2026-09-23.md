# 티어 랭킹·어드민·로딩 개선 보고서

작성일: 2026-09-23

대상 저장소: `ststat`, `staruniv`, `synergy`

## 적용 현황

2026-09-23 후속 개선으로 다음 항목을 실제 코드에 반영했다.

- 티어 월별 이력은 입력 지문이 같은 동안 마감 월을 재사용하고 현재 월은 이미 계산한 현재 랭킹 결과를 공유한다. 과거 경기·승급 이력·계산 버전이 달라지면 자동으로 전체 재계산한다.
- 티어 괴리는 뒤집힌 기준선을 단조 보정하고, 선수의 불확실성 구간이 인접 경계를 완전히 넘을 때만 최대 2단계까지 표시한다.
- 엔트리 예상 승률은 기간 탭과 분리했다. 통산 데이터에 90일 반감기를 적용한 하나의 예측을 사용하며 기간 탭은 전적 설명만 바꾼다.
- StarUniv의 385KB 통합 데이터는 `site_shell.json`과 `site_records.json`으로 분리했다. 전적 페이지에서만 큰 전적 묶음을 받는다.
- StarUniv가 Synergy 통계를 읽을 때 전체 선수가 아니라 소속 선수 ID만 조회한다.
- Synergy 팀·개인 페이지는 해당 팀 또는 선수만 서버에서 조회하며, 초기 과거 3일 선조회를 제거했다.
- Synergy와 StarUniv의 갱신 시각은 `Asia/Seoul` 기준으로 표시한다.
- 어드민 대시보드는 한 번의 RPC로 건수와 데이터 최신 상태를 조회하고, 경기 목록은 서버 페이지네이션을 사용한다.

## 1. 결론과 우선순위

1. 티어 랭킹은 **월별 과거 이력을 매 실행마다 전부 재계산하는 부분**이 가장 큰 병목이다. 현재 랭킹만 계산하면 전체 산출 시간을 약 255초에서 약 40초까지 줄일 수 있다.
2. StarUniv 초기 로딩은 모든 일반 페이지가 385KB짜리 `site_data.json` 전체를 먼저 기다리는 구조가 가장 큰 문제다. 페이지별 데이터 분리와 Supabase 조건 조회가 우선이다.
3. Synergy는 첫 화면에서 최신 일자뿐 아니라 직전 3개 일자 데이터까지 미리 가져온다. 이 선조회와 팀·개인 페이지의 전체 행 조회를 없애면 요청량을 크게 줄일 수 있다.
4. 어드민은 메뉴가 많아진 상태에서 단일 68KB 스크립트가 모든 기능을 담당한다. 운영 목적별 메뉴 재편, 서버 페이지네이션, 데이터 갱신 상태 표시가 필요하다.
5. GitHub Actions의 30분 정기 빌드는 제거했다. 외부 크론이 `workflow_dispatch`를 호출하는 구조를 사용한다.

## 2. 티어 랭킹 생성 시간

### 측정 결과

동일한 `eloboard.json`(약 13.8MB)과 `db.json`(약 1.5MB)을 사용해 로컬에서 측정했다.

| 구간 | 실행 시간 |
|---|---:|
| H2H 산출 | 31.5초(전체 측정), 27.8초(재측정) |
| 현재 랭킹만 산출(`--no-history`) | 12.7초 |
| 현재 + 과거 월별 이력 랭킹 | 223.5초 |
| 기존 전체 산출 | 약 255.1초 |
| 과거 이력을 제외한 전체 산출 | 약 40.5초 |

실행 환경과 캐시 상태에 따라 절대 시간은 달라지지만, 병목 비율은 분명하다. `build_history()`가 최근 18개월 각각에 대해 누적 경기 전체를 다시 만들고, 매월 두 단계 L-BFGS-B 최적화를 처음부터 수행한다.

### 권장 개선 순서

#### P0: 월별 이력 증분 계산

- 현재 랭킹은 매 실행 계산한다.
- 마감된 월의 `elo_rating_history`는 그대로 사용한다.
- 새 달이 시작됐을 때 직전 달 스냅샷만 확정하고, 진행 중인 달만 다시 계산한다.
- 과거 경기 수정이나 선수 승격일 변경이 있을 때만 영향받는 최초 월부터 다시 계산한다.
- 원본 경기의 최대 ID, 건수, 수정 시각과 로스터 버전을 실행 메타데이터로 저장해 무효화 범위를 판정한다.

이 방식은 랭킹 수식과 최적화 허용오차를 바꾸지 않는다. 정상 실행 경로는 약 40초 수준을 목표로 할 수 있으며, 기존 약 255초 대비 약 84% 감소 여지가 있다.

#### P1: H2H와 랭킹의 중간 파일 제거

현재 `eloboard_derived.py`가 임시 JSON을 만들고 별도 Python 프로세스 두 개를 실행한다. H2H 인덱스와 랭킹 입력을 함수 호출 또는 공통 메모리 모델로 연결하면 프로세스 시작과 대용량 JSON 직렬화 비용을 줄일 수 있다.

#### P2: 이전 월 결과로 최적화 초기화

월별 선수 레이팅은 급격히 변하지 않으므로 이전 월 해를 다음 월의 초기값으로 사용한다. 반복 횟수를 줄일 가능성이 크다. 다만 기존 결과와의 허용 오차를 고정한 회귀 검증이 먼저 필요하다.

#### P3: 최적화 허용오차 조정

현재 `maxiter=20000`, `maxfun=40000`, `ftol=1e-14`, `gtol=1e-9`로 매우 엄격하다. 대표 데이터셋에서 순위, 레이팅 차이, 티어 경계가 유지되는지 검증한 뒤 완화한다. 이 단계는 결과가 달라질 수 있어 마지막에 적용한다.

### 검증 기준

- 기존과 개선 버전의 전체 선수 순서가 동일한지 확인한다.
- 레이팅 절대 오차와 티어 경계 이동 인원을 기록한다.
- 과거 경기 추가·수정·삭제, 승격일 변경, 월 전환 시나리오를 검사한다.
- 전체 재생성 명령은 장애 복구용으로 계속 제공한다.

## 3. 어드민 페이지 구성안

### 현재 문제

- `page-admin.js` 한 파일이 대시보드부터 선수, 팀, 경기, 일정, 영상, 설정까지 모두 담당한다.
- 멤버 저장 뒤 멤버 목록과 대시보드 전체 집계를 다시 조회한다.
- 경기 500건, 영상 200건 등 고정 제한이 있고 검색·페이지네이션 기준이 메뉴마다 다르다.
- 저장한 값이 즉시 사이트에 반영되는지, 다음 외부 크론·정적 빌드 뒤 반영되는지 화면에서 구분하기 어렵다.
- 파이프라인 성공 여부와 데이터 최신 시각을 운영자가 한눈에 볼 수 없다.

### 권장 정보 구조

| 상위 메뉴 | 하위 기능 | 운영 목적 |
|---|---|---|
| 운영 현황 | 데이터 최신 시각, 외부 크론, 파이프라인·빌드 상태, 오류 | 장애와 지연 확인 |
| 선수·팀 | 선수, 팀, 시즌·라운드, 승격·티어 후보 | 로스터 운영 |
| 경기 | 경기 검색, 수정, 검증 대기, 재집계 범위 | 전적 품질 관리 |
| 콘텐츠 | 일정, 연혁, 내비게이션, 외부 도구 | 사이트 콘텐츠 관리 |
| 영상 | 채널, 추천 영상, 숨김 처리, 동기화 상태 | 영상 운영 |
| 설정·권한 | 사이트 설정, 관리자 권한, 변경 이력 | 운영 통제 |

### 화면 동작

- 첫 화면에는 총건수보다 **마지막 성공 시각, 지연 시간, 실패 작업, 다음 외부 크론 시각**을 우선 표시한다.
- 저장 버튼 옆에 `즉시 반영`, `다음 데이터 갱신 후 반영`, `다음 웹 빌드 후 반영` 배지를 표시한다.
- 선수·경기·영상 목록은 서버 검색과 커서 또는 범위 페이지네이션을 사용한다.
- 수정 화면에서 저장되지 않은 변경이 있으면 이탈 경고를 표시한다.
- 삭제와 대량 수정은 영향받는 통계·월 범위를 미리 보여준다.
- 변경 이력에는 작업자, 시각, 대상, 변경 전후 값, 재집계 여부를 남긴다.

### 코드 구성

- 공통 인증·Supabase 클라이언트·상태 표시만 코어에 둔다.
- `admin-members`, `admin-matches`, `admin-content`, `admin-videos`, `admin-settings`, `admin-operations` 단위로 모듈을 분리한다.
- 대시보드의 여러 개 count 요청은 하나의 읽기 전용 RPC나 요약 뷰로 합친다.
- 메뉴 접근 권한과 실제 쓰기 권한을 동일한 역할 정책으로 관리한다.

## 4. 느린 로딩 개선안

### StarUniv

현재 대부분의 페이지가 초기화 전에 약 385KB인 `docs/data/site_data.json` 전체 로드를 기다린다. 이 파일에는 페이지에서 사용하지 않는 선수·경기·라운드·통계까지 함께 들어 있다.

1. `site_data.json`을 `site-shell`, `members`, `matches`, `schedule` 등으로 나누고 페이지가 필요한 데이터만 요청한다.
2. 화면 골격과 내비게이션을 먼저 표시하고, 본문 데이터는 별도로 로드해 체감 대기 시간을 줄인다.
3. Synergy 통계 조회 시 최신 날짜의 전체 선수를 받은 뒤 47명을 브라우저에서 거르지 말고, StarUniv 선수 ID를 조건으로 서버에서 제한한다.
4. 실시간 방송 상태의 개별 요청 fallback에는 동시 실행 제한을 두고, Worker의 일괄 응답을 짧게 캐시한다.
5. 경기·선수 상세 데이터는 첫 화면에 필요한 구간만 조회하고 스크롤 또는 페이지 이동 때 추가한다.

### Synergy

최신 일자에는 약 1,231명의 행이 있다. 현재 첫 화면은 최신 일자를 받은 직후 과거 3개 일자도 선조회하므로 최대 4일치 전체 행을 초기 구간에서 전송한다.

1. `AVAILABLE_DATES.slice(1, 4)` 선조회를 제거한다. 비교가 필요한 시점에만 이전 월 데이터를 가져오거나 브라우저 idle 이후 1개 일자만 가져온다.
2. 팀 페이지는 `stat_date + affiliation`, 개인 페이지는 `stat_date + soop_id` 조건으로 조회한다. 전체 1,231행을 받은 뒤 필터링하지 않는다.
3. 대시보드는 카드·차트별 원본 행 대신 일별·팀별 요약 RPC 또는 materialized summary 테이블을 사용한다.
4. 날짜를 바꿨을 때 이미 받은 데이터만 메모리 캐시에 보관하고, 캐시 상한을 둔다.
5. Supabase 응답에는 화면에 쓰는 열만 포함하고 응답 압축·브라우저 캐시 정책을 확인한다.

### 적용 순서와 기대 효과

| 우선순위 | 작업 | 기대 효과 |
|---|---|---|
| P0 | Synergy 과거 3일 선조회 제거 | 초기 네트워크 요청·전송량 즉시 감소 |
| P0 | StarUniv 페이지별 데이터 분리 | 모든 일반 페이지의 첫 렌더 대기 감소 |
| P0 | 팀·개인·StarUniv 선수 조건 조회 | 행 수를 전체에서 필요한 선수만으로 축소 |
| P1 | 요약 RPC와 운영 대시보드 집계 | DB 왕복과 브라우저 연산 감소 |
| P1 | 실시간 상태 일괄 캐시·동시성 제한 | 외부 API 지연과 요청 폭주 방지 |
| P2 | 정적 자산 캐시·번들 점검 | 재방문 로딩 개선 |

브라우저 성능 개선은 배포 전후에 모바일 네트워크 기준으로 `DOMContentLoaded`, LCP, 전송 바이트, Supabase 요청 수를 같은 페이지에서 비교해야 한다.

## 5. 외부 크론과 빌드 정책

30분 GitHub Actions 스케줄은 외부 크론과 중복되므로 StarUniv와 Synergy 워크플로에서 제거했다. 두 워크플로는 다음 방식으로만 실행한다.

- 저장소 소스가 바뀔 때 자동 빌드
- 관리자가 GitHub Actions에서 수동 실행
- 외부 크론이 GitHub `workflow_dispatch` API를 호출

권장 호출 순서는 다음과 같다.

1. 외부 크론이 `ststat` 데이터 파이프라인을 실행한다.
2. 파이프라인 성공을 확인한 뒤 필요한 웹 워크플로만 실행한다.
3. StarUniv는 정적 `site_data.json`이 바뀌는 운영 데이터 변경 때 빌드한다.
4. Synergy 통계는 브라우저가 Supabase를 직접 읽으므로 통계 갱신마다 웹 빌드할 필요가 없다. 템플릿·로스터·로고처럼 정적 산출물이 바뀔 때만 빌드한다.

외부 크론은 이전 실행이 끝나지 않았으면 다음 실행을 건너뛰고, `ststat` 실패 시 웹 빌드를 시작하지 않도록 구성하는 것이 안전하다.

